import React from "react";

const Table = ({ users }) => {
  return (
    <div className="hello-world">
      Users <span className="hello-world__tech">{users}!</span>
    </div>
  );
};

export default Table;
